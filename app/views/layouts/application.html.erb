<!DOCTYPE html>
<html>
  <head>
    <title>ToolLending</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Caladea:ital,wght@0,400;0,700;1,400;1,700&family=Forum&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'https://api.mapbox.com/mapbox-gl-js/v2.15.0/mapbox-gl.css' %>
    <%= javascript_include_tag 'https://api.mapbox.com/mapbox-gl-js/v2.15.0/mapbox-gl.js' %>

  </head>

  <body class="text-gray-900">
    <%= render "shared/navbar" %>

    <div class="min-h-screen bg-gray-50 px-6 py-8 max-w-7xl mx-auto">
        <% if flash[:notice] %>
      <div id="flash-notice" class="alert alert-success position-fixed top-0 start-50 translate-middle-x mt-3 z-3">
        <%= flash[:notice] %>
      </div>
        <% elsif flash[:alert] %>
      <div id="flash-alert" class="alert alert-danger position-fixed top-0 start-50 translate-middle-x mt-3 z-3">
        <%= flash[:alert] %>
      </div>
      <% end %>

      <%= render "shared/flashes" %>
      <%= yield %>
    </div>
  </body>
</html>
